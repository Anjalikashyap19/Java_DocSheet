1.What is polymorphism and what are the types of it?

polymorphism is one of the oops cocept in which one object can able to perform different different behaviour 
at a same time.
it has two type ----
Compile time polymorphism
Run time polymorphism

compile time polymorphism also known as static polymorphism
and run time polymorphism is also known as dynamic polymorphism

2.Method binding?

it is a connection between method call statement and method definition .

3.What is compile time polymorphism and how to achieve it?
when the binding is achieved at the time of compilation then it is known as 
compile time polymorphism.
for  achieving compile time polymorphism we have 4 ways--
*By method overloading
*By constructor overloading
*By method shadowing
*By variable shadowing

***************************************************
4.What is method overloading ? WAJP for method overloading.
it is a concept of compile time polymorphism .
when a class have more than one method with the same name , but different formal arguments, then it is known as 
method overloading.
we can achieve method overloading by following ways------
by changing  data types of the arguments.
by changing order of the formal arguments.
by changing no of formal arguments.

example program------

class A{
    public void sum(int a,int b){
        System.out.println("sum is"+(a+b));
    }

    public void sum(double a, double b){
        System.out.println("sum is"+(a+b));
    }

    public static void main(String[] args){
        A a1=new A();
       a.sum(7,9);
        a.sum(5.6,3.2);
    }
}

5.Explain any one real time example of methlod overloading concept?
a very common real life example for method overloading is calculator, we use calculator for addition as well as multiple arthmatic calculation
in that if we take one operation exaple such as addition, we can able to add 2 digits also 3 and so on, also we can able to add integer type 
value as well as double and all.
so we camn say that hare method overloading concept is well knowingly used.

6.constructor overloading with example program?
constructor overloading is also a concept of compile time polymorphism,when a class more than  one constructor with 
the same name but different formal argiuments, we know it as constructor overloading.

we can achieve it is by 3 different ways----
*different number of formal arguments.
*By using different data type.
*By changing order of formal arguments.

Example-----

class A{
    A(){
        System.out.println("hello from start");
    }
    A(int a){
        System.out.println(a);
    }
    A(String a){
        System.out.println("Anjali Kr. Kashyap");
    }

    public static void main(String[] args){
        A a1= new A();  // hello from start
       
       A a2=new A(19); // 19
       A a3= new A("anjali"); // Anjali Kr. Kashyap
    }
}

